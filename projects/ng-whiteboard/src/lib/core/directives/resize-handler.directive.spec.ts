import { ResizeHandlerDirective } from './resize-handler.directive';

describe('ResizeHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
